# GUIDED dependencies
# NOTE: torch and torchvision should be installed BEFORE running `pip install -e .`
#       See README Step 1 for the recommended installation command.

# Detection frameworks (installed via `pip install -e .` from local source)
# detectron2  -- install from ./detectron2
# detrex      -- install from ./detrex

# CLIP & Vision-Language
clip @ git+https://github.com/openai/CLIP.git
timm>=0.9.0

# Parameter-efficient fine-tuning
accelerate==1.2.1
peft==0.8.0
transformers==4.20.0

# Evaluation & Metrics
pycocotools>=2.0.6
torchmetrics==1.5.2
lvis==0.5.3

# Data & Utils
numpy==1.23.5
opencv-python>=4.5.0
Pillow>=9.0.0
scipy>=1.7.0
matplotlib>=3.5.0
tqdm>=4.64.0

# Config
hydra-core>=1.3.0
omegaconf>=2.3.0

# LLM utilities (optional, for gen_level_ann.py)
# openai
